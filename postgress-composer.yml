version: '3.9'

services:

  db:
    image: postgres:16
    restart: always
    shm_size: 128mb
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: chatbot_db
      POSTGRES_PASSWORD: NVsM2R8gF0Z5

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
